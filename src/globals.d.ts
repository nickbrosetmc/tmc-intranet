/// <reference types="vite/client" />

// Inlined at build time by the tmc-build-id Vite plugin. Identifies the
// running client bundle so it can be compared against the deployed
// /version.json to detect a new release.
declare const __BUILD_ID__: string;
